server:
  port: ${CLIENT_SERVER_PORT}
#  http2:
#    enabled: true

grpc:
  server:
      address: ${CLIENT_SERVER_IP}
      port: ${CLIENT_SERVER_GRPC_PORT}

spring:
  application:
    name: letv-mas-client